# DECISIONS

Assumptions made where the spec was ambiguous, per the working rules
("choose the simplest interpretation, implement it, flag it here").

## 1. Residents are not login accounts
The spec's `users` table describes households (name, phone, address) — data
subjects, not people who sign in. Auth roles (`admin`, `station_operator`,
`reviewer`, `analyst`) belong to facility staff. So:

- `users` = residents (PII data subjects), exactly per the spec.
- `staff_accounts` = a separate table for authenticated platform users with a
  role. JWTs reference staff accounts only.

This keeps PII access control and authentication concerns fully separate.

## 2. PII protection: role-gated + audited now, encryption in Phase 4
The spec allows "encrypted at rest **or at minimum** stored in a dedicated
column set with access restricted by role". Phase 0 implements the minimum:

- PII (name/phone/address) is returned only to `admin` and `station_operator`
  (`PII_ROLES` in `app/api/deps.py`); analysts/reviewers get anonymized shapes.
- Every PII detail read writes a `pii.read` row to `audit_log`.
- Column-level encryption (pgcrypto or app-side) is deferred to Phase 4
  hardening; deterministic encryption is needed to keep the unique phone
  constraint, so it deserves its own careful change.

## 3. Celery over ARQ
Spec offered either. Celery chosen: boring, battle-tested, first-class Redis
support, and the team-size-agnostic default.

## 4. Vision model
`claude-sonnet-4-6` as specified (verified as a valid, vision-capable model ID).
Set via `VISION_MODEL` env var — never hardcoded in pipeline code.

## 5. Hand-written initial migration
`0001_initial_schema.py` is hand-written rather than autogenerated so the
schema is reviewable and deterministic. Alembic env.py imports all models, so
`alembic revision --autogenerate` works for future changes.

## 6. Bag QR / tag
The spec's `qr_code/tag_id` is a single column `tag_id` (unique) holding the QR
payload. No separate qr_code column.

## 7. `matched_brand_id` on detections
The spec says "store both raw ocr_text and matched brand" — implemented as a
nullable FK `detections.matched_brand_id -> brands.id` alongside `ocr_text`.

## 8. Idempotency key lives on captures
`captures.idempotency_key` (unique, nullable) — a retried upload with the same
key returns the existing capture instead of creating a duplicate. Enforced in
the Phase 1 capture endpoint.

## 9. Weekly profile key
`user_waste_profiles.week` implemented as `week_start` (DATE, Monday of the ISO
week) with a unique `(user_id, week_start)` constraint.

## 10. NVIDIA NIM is the default vision provider
The spec named the Anthropic API as the Phase-1 labeler, but the team's
available key is from NVIDIA (build.nvidia.com), so `VISION_PROVIDER=nvidia`
is the default with `meta/llama-4-maverick-17b-128e-instruct` (multimodal).
The Anthropic implementation is kept behind the same `VisionProvider`
interface — switching back is one env var, no code change. A `LocalYoloProvider`
stub documents the Phase-2 self-hosted path.

## 10a. Phase 1 pipeline details
- Repair round: on invalid JSON the model gets ONE retry seeing its own bad
  output; a second failure marks the capture `failed` and preserves the raw
  text in a placeholder detection row for debugging.
- Out-of-vocabulary names are demoted to `unidentified_item` with the model's
  original name kept in `subcategory` ("model said: …") — aggregation only
  ever sees known labels, but nothing is lost.
- Cost cap: a Redis daily counter incremented before every model call;
  exceeding CV_DAILY_CALL_CAP fails the capture with a clear reason.
- If the station omits session_id on upload, a CollectionSession is
  auto-created for the bag's resident (stations usually process bag-by-bag).
- Bag status: `collected` at capture upload, `processed` when analysis
  completes.

## 11. Frontend dev-mode container
The `frontend` compose service runs Vite dev server for local development. A
production build stage (nginx) is deferred until there is something to deploy.
